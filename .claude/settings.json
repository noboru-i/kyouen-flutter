{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$TOOL_INPUT\" | jq -r '.file_path | select(endswith(\".dart\"))' | xargs -r dart format"
          }
        ]
      }
    ]
  }
}